// fucu

//function suma (num1, num2) {
//return num1+num2;

//}
//console.log(suma(25,25));

//let suma = (num1, num1)=>num1+num2;
//console.log(suma(25,235));

const cuadrado = x=> x*x;
console.log (`El cuadrado de 4 es: ${cuadrado(4)}`);

//fubcuib con cuerpo

const tipo = numero => {
	  if(numero>0){
	  return `Es positivo el numero ${numero}`;
	  
	  } else if
	  (numero<0) {
	  return `Es negativo el ${numero}`;
	  }
      else {
	     return `El valor es cero, valor: ${numero}`;
	  }

};

console.log(tipo(4150));

